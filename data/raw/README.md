Place unprocessed/source material here (e.g. raw chat logs, scraped FAQs)
before curating it into `data/examples/` conversation JSONL. Nothing in
`data/raw/` is consumed directly by the training pipeline.
